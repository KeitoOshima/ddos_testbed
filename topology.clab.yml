name: ns3-test

topology:
  nodes:

    as10010:
      kind: linux
      image: alpine:latest
      exec:
        - apk add --no-cache iproute2 iputils tcpdump python3

    as4755:
      kind: linux
      image: alpine:latest
      exec:
        - apk add --no-cache iproute2 iputils tcpdump python3

    as38091:
      kind: linux
      image: alpine:latest
      exec:
        - apk add --no-cache iproute2 iputils tcpdump python3
